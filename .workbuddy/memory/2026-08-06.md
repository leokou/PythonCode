# 2026-08-06

## 修复：本地备份脚本因 Windows 保留名 `nul` 中断
- 问题：备份 `D:\Python` 的脚本 `tools/sync-GitHub/python-local-backup.py` 用 `shutil.copytree` 整盘复制，源目录 `projects/Obsidian-upload-web/nul` 是一个 0 字节的 Windows 保留设备名垃圾文件，复制时抛 `WinError 87` 并中断整个备份。
- 根因消除：删除源目录里的 `nul` 文件（用 `\\?\` 前缀绕过设备名限制删除）。
- 脚本加固：
  - `ignore` 增加跳过所有 Windows 保留名（CON/PRN/AUX/NUL/COM1-9/LPT1-9），正则 `^(CON|PRN|AUX|NUL|COM[1-9]|LPT[1-9])(\..*)?$`。
  - 新增 `copy_function=_safe_copy` 单文件复制容错：失败仅跳过并告警，不再中断整盘。
- 验证：模拟测试 `nul` 被跳过、其余正常；实跑完整备份 `python-20260806-125335` 成功（8.5MB）。
- 残留：之前失败产生的空目录 `python-20260806-1250{39,47,145}` 在 `D:\project\python备份\` 下，可清理。

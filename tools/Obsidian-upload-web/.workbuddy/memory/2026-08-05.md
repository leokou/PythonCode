# 2026-08-05

## 源编辑器「章节缩进 Section Indent」功能实现
- 需求：标题（#~####）永远顶格，正文/列表/引用/表格等所有非标题行统一固定缩进 16px，不随标题层级递增。
- 实现：新建 `frontend/section-indent.js`（独立 CM6 ViewPlugin，未污染 script.js），按行装饰：
  - ATX 标题行加 `cm-heading cm-headingN`（顶格）；frontmatter 与围栏代码块（```/~~~）内不处理。
  - 其余行加 `cm-section-indent`。
- 接线：editor.html 在 script.js 之前引入；script.js 的 `editorExtensions` 增加一行 `sectionIndentPlugin,`。
- CSS：`editor-layout.css` 的 `.cm-section-indent` 改为 `calc(12px + var(--cm-section-indent,16px))`；`theme.css` 的 `--cm-section-indent` 由 32px 改为 16px。
- 注意：此前 `cm-heading` 行类从未被 JS 应用，本次插件启用后标题会真正按 editor-layout.css 的字号/字重渲染（原本仅语法高亮上色）。构建用 `--add-data frontend;frontend` 整目录打包，新文件自动包含。

## EXE 体积 / 内存 / 性能优化（用户综合检查）
- 背景：用户要求 1) 省 EXE 体积 2) 降内存占用 3) 体验丝滑。实测 EXE=22MB（~14MB Python+依赖，~8MB 打包资源；drawnix 4.8MB 最大单项）。
- 启动 import 图已很精简：仅 `webview` 在顶层 eager；`PIL`/`msal`/`requests` 均为懒加载。真正的内存/启动开销来自 **8 个隐藏 WebView2 窗口在启动即全部创建** + `--onefile` 每次启动解包 22MB 到临时目录。
- 决策（用户确认）：onedir 单文件夹 + 懒加载画布/To Do + 保留完整 Drawnix。
- 已实施（build.bat，AI 不自动打包，用户手动 `build.bat`）：
  - `--onefile` → `--onedir`（启动免解包、峰值内存更低；产物 `dist/Obsidian-upload/` 文件夹）。
  - 排除未用模块：tkinter / PIL._imagingtk / PIL.ImageTk / PIL.ImageQt / pystray._gtk|_cocoa|_appkit|_qt|_dummy / gi / PyQt*/PySide*/cefpython3 / pytest / tests / test。
  - 用 `--hidden-import=pystray._win32` 替代 `--collect-submodules=pystray`（不再强制打包全部平台后端）。
  - dist 路径清理改为删 `dist\Obsidian-upload` 文件夹。
- 已实施（代码，内存/启动优化）：
  - `main.py` 新增模块级 `_ensure_canvas()`（懒创建画布窗口 + 启动 HTTP 服务，首次 open_canvas / import_markdown_to_canvas 时触发）；删除启动期画布预创建块与 `todo_window.create()` 预创建调用。
  - `api.py` 的 `open_canvas` / `import_markdown_to_canvas` 改为先调 `_main._ensure_canvas()`。
  - 效果：启动不再加载重型 Drawnix(React) 应用与 msal/数据库，少 2 个常驻渲染进程；首次开画布/To Do 有 ~200–500ms 延迟（show()/close() 已有 None 守卫，低回归风险）。
  - 校验：`py_compile` 通过；CLAUDE.md 与 main.py docstring 已同步（onedir + 懒加载描述）。
- 未做（用户选保留完整 Drawnix）：精简重建 Drawnix 构建（可再省 ~2–3MB，但需手动重建、有回归风险）。

## 新增 build-onedir.bat / build-exe.bat
- 用户要求拆分两个构建脚本。已新建 `build-onedir.bat`（= 当前 onedir 配置，推荐，输出 `dist/Obsidian-upload/` 文件夹）与 `build-exe.bat`（单文件回退，输出 `dist/Obsidian-upload.exe`）。
- 两者均输出于项目根 `D:\Python\tools\Obsidian-upload-web\dist\` 下，即 onedir 文件夹就在该项目目录内。
- `build-exe.bat` 区别：仅 `--onefile` 替代 `--onedir`，清理行删 `dist\Obsidian-upload.exe` 而非文件夹，成功提示指向单文件。优化排除项/隐藏导入与 onedir 版完全一致。
- 注：原 `build.bat` 当前内容等同 onedir 版（未删除，保留向后兼容）。如不需要可删。

## 双bat双击"没反应"排障（重要坑）
- 现象：双击 `build-onedir.bat`/`build-exe.bat` 窗口一闪而过、不生成产物。
- 根因：Write 工具生成的 .bat 是 **LF 行尾**（CRLF=0）。Windows 批处理的 `^` 续行在 LF 下失效，第一条 `pyinstaller ... ^` 长命令被截断 → 语法错误 → 静默退出。
- 修复：用 python 把两个 .bat 的 `\n` 统一替换为 `\r\n`（CRLF）。修复后 `where pyinstaller` 命中 Python314 的 pyinstaller.exe，且 `python314 -c "import webview,PIL,pystray,requests,msal,keyboard"` 全部可导入 → 构建可达。
- 经验：**凡用 Write 生成 Windows .bat/cmd，必须转 CRLF**，否则 `^` 续行会静默失败。勿加 UTF-8 BOM（会让首行 `@echo off` 带 BOM 前缀而失效）。
- 输出位置确认：均落在 `D:\Python\tools\Obsidian-upload-web\dist\`（onedir→`dist\Obsidian-upload\` 文件夹；onefile→`dist\Obsidian-upload.exe`）。
- CLAUDE.md 约束 AI 不自动打包，故未实跑，由用户双击验证。

## bincache 损坏导致 Build failed（续上）
- 现象：CRLF 修好后构建真跑起来，但在 COLLECT 阶段报 `PyInstaller bincache may be corrupted` + `IndentationError: unexpected indent`（`<string>` line 302 `b'\xac<]\x8b'`）。
- 根因：之前 taskkill 强杀构建留下脏的全局 bincache；`pyinstaller --clean` 只清 `build/`+`dist/`，**不**清全局 bincache（`%LOCALAPPDATA%\pyinstaller\bincache01py31464bit`）。
- 修复：删除 `C:/Users/leokou/AppData/Local/pyinstaller/bincache01py31464bit`（仅强制重分析，无数据损失）；重新双击 bat 即可（bat 自带 `--clean`）。
- 坑：本环境的 safe-delete 包装只认「盘符+正斜杠」绝对路径（`D:/...`/`C:/...`）。`C:\...`（混反斜杠）和 `/c/...`（MSYS 形式）都会被误判为相对路径而拒绝删除；必须用 `rm -rf "C:/Users/..."` 才成功。
- 结论：首次构建务必等其自然完成，勿中途 taskkill，避免再次污染 bincache。

## UPX 5.2.0 压不了 .NET 程序集 → 二次 Build failed（重要）
- 现象：清缓存重跑后在 EXE 阶段又报 `bincache may be corrupted` + `SyntaxError: invalid syntax`，首行 `upx: ...\pythonnet\runtime\System.IO.Compression.ZipFile.dll: CantPackException: unexpected value in PE header`。
- 根因：UPX 5.2.0 无法压缩 .NET 托管程序集（pythonnet/runtime 下 140+ 个 `System.*.dll`/`Microsoft.*.dll`/`netstandard.dll`/`Python.Runtime.dll`）。失败写坏 bincache → 连锁报 corrupted。
- 关键反直觉点：**不能 `--exclude-module pythonnet`**。`webview/platforms/edgechromium.py` 运行时 `import clr`（pythonnet）并 `clr.AddReference('Microsoft.Web.WebView2.Core.dll')` 加载 WebView2——排除会让所有窗口黑屏。pywebview 把 pythonnet 列为 Required-by 即此理。
- 修复（已写入 build-onedir.bat / build-exe.bat）：保留 pythonnet，加 4 条 `--upx-exclude` 跳过 .NET 程序集：
  `System.*.dll` / `Microsoft.*.dll` / `netstandard.dll` / `Python.Runtime.dll`。原生 DLL（pywin32/Pillow 等）仍 UPX 压缩，保留体积收益。
- 验证：pythonnet/runtime 目录已枚举，上述 4 个 glob 覆盖其全部 DLL（System.*、Microsoft.Win32.Primitives、Python.Runtime、netstandard）。
- 注：本回合 corrupted bincache 已再次 `rm -rf "C:/Users/leokou/AppData/Local/pyinstaller/bincache01py31464bit"` 清掉，用户可直接重跑 build-onedir.bat。

## 编辑区/预览区标题字号优化（H1–H4 减小一号，需求第 1 项）
- 诉求：`#` 一~四级标题太大，统一减小一号先看效果。
- 编辑区（CodeMirror）：标题字号由 `frontend/theme.css` 的 `--cm-heading1-size … --cm-heading4-size` 全局变量控制（各编辑器主题只覆盖颜色不改字号）。已改为 1.8→1.5 / 1.5→1.3 / 1.25→1.15 / 1.1→1.0（em）。单点改动即全局生效。
- 预览区：原字号分散在 `style.css` 基础值(26/22/19px) + 50+ 预览主题硬编码（普遍 30–35px）。预览主题在 style.css 之后动态加载，单点覆盖须用 `!important`。已在 `style.css` 末尾追加 `.preview-body h1..h4` 的 `!important` 覆盖（24/20/17/15px）。
- 取舍：预览区当前用 `!important` 强制统一更小值，会抹平各主题差异。若用户要保留主题差异、仅等比缩小，需改为逐主题缩放（更大改动）。已做 before/after 对比页 `frontend/heading-effect-demo.html` 供先看效果。
- 红线校验：本次属用户明确要求的显示优化，不违反「不随意改预览区」红线。

## 标题字号二次下调（"再小一号"）
- 在上一轮基础上再降一号：theme.css 变量 1.5→1.3 / 1.3→1.15 / 1.15→1.0 / 1.0→0.9 (em)；style.css !important 覆盖 24→21 / 20→17 / 17→14 / 15→13 (px)。heading-effect-demo.html 右列已同步。

## 目录(Outline)点击不高亮 + 定位失效修复
- 现象：点击目录项后高亮不停留在上次光标位置、不切换选中态；期望点击即高亮并把编辑区+预览区定位到该标题。
- 根因：`frontend/js/outline.js` 的 click 处理器只调 `scrollToLine(line)`（滚动编辑/预览），从未更新 `activeLine` 也没给被点项加 `.active` 类；高亮仅由光标移动的 `highlightAtPos` 驱动。
- 修复：抽出 `setActive(line)` 统一切换 active 类 + 把该项滚入目录可视区（复用 `scrollActiveIntoView`）；click 处理器先 `setActive(line)` 再 `scrollToLine(line)`；`highlightAtPos` 改为复用 `setActive`。改法只在独立模块内，未触碰预览区/编辑区双向同步代码（符合红线）。
- 部署：已同步到 `dist/Obsidian-upload/_internal/frontend/js/outline.js`（冻结 EXE 加载此副本），dist 不被 git 跟踪故无需提交。
